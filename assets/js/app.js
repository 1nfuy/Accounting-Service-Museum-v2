/* ========================================
   ИНИЦИАЛИЗАЦИЯ ПРИЛОЖЕНИЯ
   ======================================== */
document.addEventListener('DOMContentLoaded', () => {
	initNavbar();
	initTimeline();
	renderLeaders();
	renderWarHero();
	renderExhibits();
	initFilters();
	initCalculator();
	initScrollAnimations();
	initModal();
	initExhibitModal();
	initSmoothScroll();
});