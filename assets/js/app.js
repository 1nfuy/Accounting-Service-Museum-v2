/* ========================================
   ИНИЦИАЛИЗАЦИЯ ПРИЛОЖЕНИЯ
   ======================================== */
document.addEventListener('DOMContentLoaded', () => {
	initNavbar();
	initTimeline();
	renderLeaders();
	renderExhibits();
	initFilters();
	initCalculator();
	initScrollAnimations();
	initModal();
	initExhibitModal();
	initSmoothScroll();
});